<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/mint_primary</item>
        <item name="colorPrimaryDark">@color/mint_primary_dark</item>
        <item name="colorAccent">@color/mint_accent</item>
        <item name="android:windowBackground">@color/mint_background</item>
        <item name="android:textColorPrimary">@color/mint_text_primary</item>
        <item name="android:textColorSecondary">@color/mint_text_secondary</item>
        
        <!-- Стиль для выпадающих списков -->
        <item name="autoCompleteTextViewStyle">@style/MintAutoCompleteTextView</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
    
    <!-- Стиль для AutoCompleteTextView -->
    <style name="MintAutoCompleteTextView" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:background">@color/mint_background_light</item>
        <item name="android:textColor">@color/mint_text_primary</item>
        <item name="android:textColorHint">@color/mint_text_secondary</item>
        <item name="android:padding">12dp</item>
    </style>
</resources>